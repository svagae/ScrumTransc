import React from 'react';

const Header = () => (
  <header>
    <div className="header-title">LiveScript</div>
    <div className="header-subtitle">Real-Time Transcription App</div>
  </header>
);

export default Header;